# requests <2.33.0 / python-dotenv <1.2.2 carry known CVEs — the `~=` pins
# previously blocked the fixed releases. Floors raised to the patched versions.
requests>=2.33.0
pytz~=2024.1
python-dotenv>=1.2.2
python-dateutil~=2.9.0.post0
python-telegram-bot[job-queue]~=22.0
openpyxl>=3.0.10

# Web dashboard
fastapi>=0.109.0
itsdangerous>=2.1.0
uvicorn[standard]>=0.27.0
jinja2>=3.1.3
orjson>=3.9.0
httpx>=0.27.0
nest-asyncio>=1.6.0
slowapi>=0.1.9

# DuckDB analytics
duckdb==1.5.5
pandas>=2.0.0

# Job scheduling
apscheduler>=3.10.0

# ML prediction
lightgbm>=4.0.0
scikit-learn>=1.3.0
joblib>=1.3.0

# Caching

# Weekly report card (core/weekly_report_image.py). Pillow alone, on purpose:
# matplotlib would draw that chart in a tenth of the code and add ~100 MB to an
# image that already runs against a memory ceiling, for one picture a week.
# Needs fonts-dejavu-core in the image — see Dockerfile.web.
pillow>=12.0.0

# NOTE: test/dev dependencies live in requirements-dev.txt — they are
# intentionally NOT installed into the production Docker images.

# Chat assistant
anthropic>=0.40.0
sse-starlette>=2.0.0
meilisearch>=0.31.0
