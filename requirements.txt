# requests <2.33.0 / python-dotenv <1.2.2 carry known CVEs — the `~=` pins
# previously blocked the fixed releases. Floors raised to the patched versions.
requests>=2.33.0
pytz~=2024.1
python-dotenv>=1.2.2
python-dateutil~=2.9.0.post0
python-telegram-bot[job-queue]~=22.0
openpyxl>=3.0.10

# Web dashboard
fastapi>=0.109.0
itsdangerous>=2.1.0
uvicorn[standard]>=0.27.0
jinja2>=3.1.3
orjson>=3.9.0
httpx>=0.27.0
nest-asyncio>=1.6.0
slowapi>=0.1.9

# Postgres (step 05 of the migration). Nothing reads or writes it yet —
# core/pg.py opens a pool on first use and never at import.
#
# NOT 0.30.0, which is what the sibling bot pins. That release predates
# Python 3.14 and ships no cp314 wheel, so pip falls back to building the C
# extension from source — and `python:3.14-slim` has no gcc. Verified: the
# image build fails with "error: [Errno 2] No such file or directory: 'gcc'".
# 0.31.0 is the first release with a cp314 wheel, and it installs and imports
# in the production base image.
asyncpg==0.31.0

# DuckDB analytics
duckdb==1.5.5
pandas>=2.0.0

# Job scheduling
apscheduler>=3.10.0

# ML prediction
lightgbm>=4.0.0
scikit-learn>=1.3.0
joblib>=1.3.0

# Caching

# Weekly report card (core/weekly_report_image.py). Pillow alone, on purpose:
# matplotlib would draw that chart in a tenth of the code and add ~100 MB to an
# image that already runs against a memory ceiling, for one picture a week.
# Needs fonts-dejavu-core in the image — see Dockerfile.web.
pillow>=12.0.0

# NOTE: test/dev dependencies live in requirements-dev.txt — they are
# intentionally NOT installed into the production Docker images.

# Chat assistant
anthropic>=0.40.0
sse-starlette>=2.0.0
meilisearch>=0.31.0
